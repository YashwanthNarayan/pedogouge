{
  "name": "@pedagogue/infra-tests",
  "version": "0.0.1",
  "private": true,
  "description": "RLS policy matrix tests — run against a real (local or staging) Supabase instance",
  "scripts": {
    "test:rls": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.46.0",
    "vitest": "^2.1.0",
    "typescript": "^5.6.3",
    "@types/node": "^20.0.0"
  }
}

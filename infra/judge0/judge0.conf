# Judge0 configuration — fill in secrets before deploy (T3-03)
# Never commit with real values — use DigitalOcean secrets or Vault

REDIS_PASSWORD=REPLACE_ME
POSTGRES_PASSWORD=REPLACE_ME

AUTHN_HEADER=X-Auth-Token
AUTHN_TOKEN=REPLACE_ME

# Execution limits (plan P.3)
CPU_TIME_LIMIT=15
MEMORY_LIMIT=256000
STORAGE_LIMIT=64000
MAX_PROCESSES_AND_OR_THREADS=60
MAX_QUEUE_SIZE=100

# No network for student code
ENABLE_NETWORK=false

# Webhook callbacks
CALLBACKS_MAX_TRIES=3
CALLBACKS_TIMEOUT=15

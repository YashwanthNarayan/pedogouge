# Standup — 2026-04-21 (Day 1)

---

## Who am I coding for?

**Primarily P2 (AI + Voice + Web UI lead).**
I also handled the P1 tasks that were the Day 1 prerequisite (extension scaffold + @tutor chat participant), since those needed to exist for the system to hang together. Detailed breakdown below.

---

## What is done

### packages/shared — co-owned (contract-v1, frozen)
- [x] All 6 Zod schemas (plan Appendix A, verbatim):
  - `ProjectBlueprint` — full intake output with starterRepo
  - `ConceptNode` — mastery, decay, strugglePattern enum
  - `ASTDiagnostic` — ruleId, severity, lessonLink
  - `InterventionDecision` — tier 1-5, deliveryChannel
  - `InterviewContext` — phases, answers, injectedBug, counterfactuals
  - `VerifiableCredentialSubject` — proofOfStruggle, radar, interviewSummary
- [x] `api.ts` — every Next.js route's request/response Zod types (~20 routes)
- [x] `channels.ts` — Supabase Realtime channel name constants
- [x] `events.ts` — EventKind enum + per-kind payload types
- [x] `gen-schemas.ts` — generates JSON schema files from Zod (6 files in `src/json-schema/`)
- [x] 44 unit tests — valid parse, roundtrip, 3 invalid cases per schema
- [x] `vitest.config.ts` for the package

### apps/web/lib/anthropic — P2
- [x] `models.ts` — Models const (opus/sonnet/haiku IDs), Pricing per model, `estimateCostUsd()`
- [x] `errors.ts` — 6 typed error classes: CanaryEchoedError, RefusalViolationError, SchemaParseError, RateLimitError, BudgetExceededError, APIError
- [x] `canary.ts` — `generateCanary()` (CNR-{16hex}), `injectCanary()`, `verifyResponse()`
- [x] `system-prompt.ts` — 4-block assembler with multi-breakpoint cache_control (1h preamble+blueprint, 5m graph+events), 7-rule refusal BASE_PREAMBLE, user_input tag instruction
- [x] `client.ts` — `call<T>()`, `callStream()`, `callWithCitations()`, `withRetry()`, `wrapUserContent()`, 6-pattern output filter (BANNED_PATTERNS), `zodToJsonSchema()` helper
- [x] `intake-pipeline.ts` — 2-call parallel intake: fan-out (Opus, tool_choice:any → 3 tools) + synthesis (Opus, output_schema:ProjectBlueprint); specialist routing (opus/sonnet/haiku); guard for exactly 3 tool_use blocks
- [x] `__tests__/client.test.ts` — 36 tests
- [x] `__tests__/intake-pipeline.test.ts` — 17 tests

### apps/web/lib/embeddings — P2
- [x] `voyage-client.ts` — Voyage AI client, voyage-code-3, 1024-dim, 128-chunk batching, 3-retry exponential backoff, AbortSignal support

### apps/web/app/api/intake — P2
- [x] `route.ts` — POST handler: IntakeRequest zod validation, auth check, calls runIntake(), fires Voyage embeddings (non-blocking in dev), returns {sessionId, blueprint}

### packages/extension — P1 (T1-01 + T1-02)
- [x] Extension scaffold — package.json with all contributes (chatParticipants, commands, configuration), tsconfig, esbuild.js, .vscodeignore
- [x] `extension.ts` — activate() registers all 11 commands (stubs), wires @tutor chat participant
- [x] `backend/client.ts` — BackendClient with `request<T>()` and `streamSSE()` async generator; reads backendUrl from VS Code config; handles 401/429
- [x] `chat/slash-commands.ts` — /explain, /debug, /review, /defend handlers; abortSignalFromToken; handleStreamError
- [x] `chat/participant.ts` — createParticipantHandler factory; auth check (SecretStorage); mock mode (PEDAGOGUE_MOCK_CHAT); history building (last 10 turns); SSE streaming; 401/429/AbortError handling
- [x] Extension builds: `dist/extension.js` via esbuild (12K) ✓

---

## Test counts
| Package | Tests |
|---------|-------|
| packages/shared | 44 |
| apps/web | 53 (36 client + 17 intake) |
| **Total** | **97** |

All 97 passing. `pnpm test` green across monorepo.

---

## What is NOT done yet (remaining Day 1 tasks)

### P2 (mine)
- [ ] T2-04 — Dual-track curriculum generator (Citations API + pgvector KNN) — blocked on T3-01 (Supabase migrations) for real `match_chunks` RPC; can stub
- [ ] T2-05 — Session overview page + lesson renderer + credential page (web UI)

### P1 (extension)
- [ ] T1-03 — Auth: SecretStorage, AuthenticationProvider, PKCE URI handler
- [ ] T1-04 — Mobile app scaffold (Expo)
- [ ] T1-05 — Tree-sitter WASM loader + 5 grammars
- [ ] T1-06 through T1-15 — all remaining extension tasks

### P3 (backend/infra — not touched yet)
- [ ] T3-01 — Supabase schema migrations + pgvector + HNSW (HIGH PRIORITY — unblocks everything)
- [ ] T3-02 — Full RLS matrix + test:rls CI
- [ ] T3-03 — Judge0 VM provision
- [ ] T3-04 — Voyage ingest (500 KB chunks)
- [ ] T3-05 — GitHub OAuth + extension token exchange
- [ ] T3-06 through T3-20 — all remaining infra tasks

### Still needed (global)
- [ ] `git init && git add . && git commit` — user needs to run this (requires Xcode CLI tools: `xcode-select --install`)

---

## Day 1 Gate status
> **Gate:** `@tutor hello` returns streamed response in VS Code

- Extension builds ✓
- Chat participant wired ✓  
- Mock mode works (PEDAGOGUE_MOCK_CHAT=true) ✓
- Real backend streaming: wired but backend route /api/chat not yet implemented (T3/P3 work)

**Gate: ~60% done.** Mock path works end-to-end; real backend path needs the Supabase + auth layer (P3).
